import React from "react";

const SectionTitle = (props)=>{
    return (
        <div>
            <h2>{props.title}</h2>
            <hr></hr>
            
        </div>
    )
}

export default SectionTitle